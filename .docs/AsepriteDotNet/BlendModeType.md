# BlendMode enumeration

Defines the blend mode used when blending two color values.

```csharp
public enum BlendMode
```

## Values

| name | value | description |
| --- | --- | --- |
| Normal | `0` | Normal blend mode is the standard blend mode that takes the top layer alone without mixing any color from the layer beneath it. |
| Multiply | `1` | Multiply blend mode that takes the RGB component values of each pixel from the top layer and multiplies them with the RGB component values of the cooresponding pixel from the bottom layer. |
| Screen | `2` | Screen blend mode takes the RGB component values of each pixel from the top and bottom layer and inverts them, then multiples the RGB component values of each pixel from the top layer with the RGB component values of hte cooresponding pixel from the bottom layer, then the RGB component value of each resulting pixel is inverted again. |
| Overlay | `3` | Overlay blend combines the Multiply and Screen blend modes based on the tonal value of the bottom layer. If the bottom layer is darker than 50% gray, then the tonal values are multiplied; otherwise, they get screened. In both cases the resulting value is doubled after. |
| Darken | `4` | Darken blend retains the smallest of each RGB component for each cooresponding pixel from the top and bottom layer. |
| Lighten | `5` | Lighten blend retains the largest of each RGB component for each cooresponding pixel from the top and bottom layer. |
| ColorDodge | `6` | Color Dodge blend divides each pixel from the bottom layer with the cooresponding inverted pixel from the top layer. |
| ColorBurn | `7` | Color Burn blend divides each inverted pixel from the bottom layer with the coorespoding pixel from the top layer, then inverts the resulting value. |
| HardLight | `8` | Hard Light blend combines the Multiply and Screen blend modes based on the tonal value of the top layer. If the top layer is darker than 50% gray, then the tonal values are multiplied; otherwise, they get screened. In both cases the resulting value is doubled after. |
| SoftLight | `9` | Soft Light blend modulates the tonal values of the bottom layer by the tonal values of the top layer. |
| Difference | `10` | Difference blend returns the absolute value in the difference between RGB component value of each pixel in the top layer from the RGB component value in the coorespoding pixel in the bottom layer. |
| Exclusion | `11` | Exclusion blend mode takes the sum of the RGB component values of each pixel in the top layer with the RGB component value of each coorespoding pixel in the bottom layer, then subtracts the doubled product of top and bottom layer |
| Hue | `12` | Hue blend mode preserves the luma and chroma of each pixel in the bottom layer and adopts the hue of the corresponding pixel in the top layer |
| Saturation | `13` | Saturation blend mode preserves the luma and hue of each pixel in the bottom layer and adopts the chroma of the corresponding pixel in the top layer |
| Color | `14` | The color blend mode preserves the luma of each pixel in the bottom layer and adopts the hue and chorma of cooresponding pixel in the top layer. |
| Luminosity | `15` | Luminosity blend mode preserves the hue and chroma of each pixel in the bottom layer and adopst the luma of the corresponding pixel in then top layer |
| Addition | `16` | Addition blend mode adds the RGB component values of each pixel from the top layer with the RGB component values of each cooresponding pixel in the bottom layer. |
| Subtract | `17` | Subtract blend mode subtracts the RGB compoennt values of each pixel from the top layer from the RGB component values of each cooresponding pixel in the bottom layer. |
| Divide | `18` | Divide blend mode divides the RGB component values of each pixel from the bottom layer by the RGB component values of each cooresponding pixel from the top layer. |

## See Also

* namespace [AsepriteDotNet](../AsepriteDotNetNamespace)
* assembly [AsepriteDotNet](../AsepriteDotNetAssembly)
* [BlendMode.cs](https://github.com/AristurtleDev/AsepriteDotNet/tree/main/source/AsepriteDotNet/BlendMode.cs)

<!-- DO NOT EDIT: generated by xmldocmd for AsepriteDotNet.dll -->
